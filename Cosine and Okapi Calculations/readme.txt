We run the code in PyCharm, Python 3.7.
We crated Python Configuration.
We chose the location of code as a Script path.
For environment, We chose Python 3.7

It runs in PyCharm, Python 3.7 and also these codes which are below are applied to run in harveyv:
	to install:
		pip3 install --user -U nltk
	to run:
		python3 cosandok.py
	